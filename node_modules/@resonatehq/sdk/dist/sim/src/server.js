"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.ServerProcess = void 0;
const server_1 = require("../../dev/server");
const util = __importStar(require("../../src/util"));
const simulator_1 = require("./simulator");
class ServerProcess extends simulator_1.Process {
    iaddr;
    clock;
    server = new server_1.Server();
    constructor(clock, iaddr) {
        super(iaddr);
        this.iaddr = iaddr;
        this.clock = clock;
    }
    tick(tick, messages) {
        this.log(tick, "[recv]", messages);
        const responses = [];
        for (const message of messages) {
            util.assert(message.target.iaddr === this.iaddr);
            if (message.isRequest()) {
                let res;
                try {
                    res = { res: this.server.process(message.data, this.clock.time) };
                }
                catch (err) {
                    res = { err: err };
                }
                responses.push(message.resp(res));
            }
        }
        for (const message of this.server.step(this.clock.time)) {
            const url = new URL(message.recv);
            let target;
            if (url.username === "any") {
                target = url.pathname === "" ? (0, simulator_1.anycast)(url.hostname) : (0, simulator_1.anycast)(url.hostname, url.pathname.slice(1));
            }
            else if (url.username === "uni") {
                target = (0, simulator_1.unicast)(url.pathname.slice(1));
            }
            else {
                throw new Error(`not handled ${url}`);
            }
            const msg = new simulator_1.Message((0, simulator_1.unicast)(this.iaddr), target, message.msg, { requ: true });
            responses.push(msg);
        }
        this.log(tick, "[send]", responses);
        return responses;
    }
}
exports.ServerProcess = ServerProcess;
//# sourceMappingURL=server.js.map