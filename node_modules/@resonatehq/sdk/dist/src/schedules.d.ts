import type { Network, ScheduleRecord } from "./network/network";
export declare class Schedules {
    private network;
    constructor(network?: Network);
    get(id: string): Promise<ScheduleRecord>;
    create(id: string, cron: string, promiseId: string, promiseTimeout: number, { ikey, description, tags, promiseHeaders, promiseData, promiseTags, }?: {
        ikey?: string;
        description?: string;
        tags?: Record<string, string>;
        promiseHeaders?: Record<string, string>;
        promiseData?: string;
        promiseTags?: Record<string, string>;
    }): Promise<ScheduleRecord>;
    delete(id: string): Promise<void>;
    search(id: string, { limit }?: {
        limit?: number;
    }): AsyncGenerator<ScheduleRecord[], void>;
}
//# sourceMappingURL=schedules.d.ts.map