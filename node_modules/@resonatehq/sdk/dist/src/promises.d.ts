import type { CallbackRecord, DurablePromiseRecord, Network, TaskRecord } from "./network/network";
export declare class Promises {
    private network;
    constructor(network?: Network);
    get(id: string): Promise<DurablePromiseRecord>;
    create(id: string, timeout: number, { ikey, strict, headers, data, tags, }?: {
        ikey?: string;
        strict?: boolean;
        headers?: Record<string, string>;
        data?: string;
        tags?: Record<string, string>;
    }): Promise<DurablePromiseRecord>;
    createWithTask(id: string, timeout: number, pid: string, ttl: number, { ikey, strict, headers, data, tags, }?: {
        ikey?: string;
        strict?: boolean;
        headers?: Record<string, string>;
        data?: string;
        tags?: Record<string, string>;
    }): Promise<{
        promise: DurablePromiseRecord;
        task?: TaskRecord;
    }>;
    resolve(id: string, { ikey, strict, headers, data, }?: {
        ikey?: string;
        strict?: boolean;
        headers?: Record<string, string>;
        data?: string;
    }): Promise<DurablePromiseRecord>;
    reject(id: string, { ikey, strict, headers, data, }?: {
        ikey?: string;
        strict?: boolean;
        headers?: Record<string, string>;
        data?: string;
    }): Promise<DurablePromiseRecord>;
    cancel(id: string, { ikey, strict, headers, data, }?: {
        ikey?: string;
        strict?: boolean;
        headers?: Record<string, string>;
        data?: string;
    }): Promise<DurablePromiseRecord>;
    search(id: string, { state, limit }?: {
        state?: "pending" | "resolved" | "rejected";
        limit?: number;
    }): AsyncGenerator<DurablePromiseRecord[], void>;
    callback(promiseId: string, rootPromiseId: string, recv: string, timeout: number): Promise<{
        promise: DurablePromiseRecord;
        callback: CallbackRecord | undefined;
    }>;
    subscribe(id: string, promiseId: string, timeout: number, recv: string): Promise<{
        promise: DurablePromiseRecord;
        callback: CallbackRecord | undefined;
    }>;
}
//# sourceMappingURL=promises.d.ts.map