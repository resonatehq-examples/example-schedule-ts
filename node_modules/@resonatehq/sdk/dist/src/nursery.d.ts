export declare class Nursery<E, R> {
    private q;
    private f;
    private c;
    private e?;
    private r?;
    private holds;
    private running;
    private completed;
    constructor(f: (n: Nursery<E, R>) => void, c: (e?: E, r?: R) => void);
    hold(f: (f: () => void) => void): void;
    cont(): void;
    done(err?: E, res?: R): void;
    all<T, U, E>(list: T[], func: (item: T, done: (err?: E, res?: U) => void) => void, done: (err?: E, res?: U[]) => void): void;
    private enqueue;
    private execute;
    private complete;
}
//# sourceMappingURL=nursery.d.ts.map