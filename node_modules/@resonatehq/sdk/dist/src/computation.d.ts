import type { Clock } from "./clock";
import type { Handler } from "./handler";
import type { Heartbeat } from "./heartbeat";
import type { CallbackRecord, DurablePromiseRecord, Network } from "./network/network";
import type { OptionsBuilder } from "./options";
import { type Processor } from "./processor/processor";
import type { Registry } from "./registry";
import type { Task } from "./resonate-inner";
import { type RetryPolicyConstructor } from "./retries";
import type { Span, Tracer } from "./tracer";
import type { Callback } from "./types";
export type Status = Completed | Suspended;
export type Completed = {
    kind: "completed";
    promise: DurablePromiseRecord;
};
export type Suspended = {
    kind: "suspended";
    callbacks: CallbackRecord[];
};
export declare class Computation {
    private id;
    private pid;
    private ttl;
    private clock;
    private unicast;
    private anycast;
    private network;
    private handler;
    private retries;
    private registry;
    private dependencies;
    private optsBuilder;
    private verbose;
    private heartbeat;
    private processor;
    private span;
    private spans;
    private seen;
    private processing;
    constructor(id: string, unicast: string, anycast: string, pid: string, ttl: number, clock: Clock, network: Network, handler: Handler, retries: Map<string, RetryPolicyConstructor>, registry: Registry, heartbeat: Heartbeat, dependencies: Map<string, any>, optsBuilder: OptionsBuilder, verbose: boolean, tracer: Tracer, span: Span, processor?: Processor);
    process(task: Task, done: Callback<Status>): void;
    private processClaimed;
    private processGenerator;
    private processFunction;
    private processLocalTodo;
    private processRemoteTodo;
}
//# sourceMappingURL=computation.d.ts.map