import type { StepClock } from "../../src/clock";
import type { Encoder } from "../../src/encoder";
import type { Message as NetworkMessage, Request, Response } from "../../src/network/network";
import type { Registry } from "../../src/registry";
import { Message, Process, type Random } from "./simulator";
interface DeliveryOptions {
    charFlipProb?: number;
}
export declare class WorkerProcess extends Process {
    readonly iaddr: string;
    readonly gaddr: string;
    private clock;
    private network;
    private registry;
    private resonate;
    constructor(prng: Random, clock: StepClock, encoder: Encoder, registry: Registry, { charFlipProb }: DeliveryOptions, iaddr: string, gaddr: string);
    tick(tick: number, messages: Message<{
        err?: any;
        res?: Response;
    } | NetworkMessage>[]): Message<Request>[];
}
export {};
//# sourceMappingURL=worker.d.ts.map