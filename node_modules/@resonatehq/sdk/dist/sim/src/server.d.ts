import { Server } from "../../dev/server";
import type { StepClock } from "../../src/clock";
import type { Message as NetworkMessage, Request, Response } from "../../src/network/network";
import { Message, Process } from "./simulator";
export declare class ServerProcess extends Process {
    readonly iaddr: string;
    private clock;
    server: Server;
    constructor(clock: StepClock, iaddr: string);
    tick(tick: number, messages: Message<Request>[]): Message<{
        err?: any;
        res?: Response;
    } | NetworkMessage>[];
}
//# sourceMappingURL=server.d.ts.map